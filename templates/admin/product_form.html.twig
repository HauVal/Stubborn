{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ form.vars.value.id ? 'Modifier le produit' : 'Ajouter un produit' }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">{{ form.vars.value.id ? 'Modifier' : 'Ajouter' }}</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_products') }}">Retour à la liste des produits</a>
{% endblock %}
